{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main RasterSVG window.",
  "windows": ["main"],
  "permissions": ["dialog:default"]
}
