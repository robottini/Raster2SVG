{
  "name": "rastersvg",
  "version": "0.1.0",
  "private": true,
  "description": "RasterSVG desktop shell and migration workspace.",
  "license": "GPL-2.0-or-later",
  "packageManager": "pnpm@11.7.0",
  "type": "module",
  "scripts": {
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:app": "tauri build --bundles app",
    "legacy:serve": "python3 -m uvicorn backend.main:app --host 127.0.0.1 --port 8000",
    "baseline": "python3 tools/generate_legacy_baseline.py"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
